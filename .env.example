# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=   # server only: build-time read + admin writes

# Anthropic (server only, for /api/chat)
ANTHROPIC_API_KEY=

# Used by the revalidation route handler to authorize on-demand ISR
REVALIDATE_SECRET=
